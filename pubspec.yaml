name: enhanced_containers
description: A collection of custom containers for Dart
version: 1.0.0

publish_to: "none"

environment:
    sdk: ">=2.17.6 <3.0.0"

dependencies:
    flutter:
        sdk: flutter
    firebase_database: ^10.0.3
    nanoid: ^1.0.0

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^2.0.0
